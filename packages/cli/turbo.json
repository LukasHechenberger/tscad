{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["out/**", "README.md", "../../apps/docs/content/docs/\\(cli\\)/*.mdx"]
    },
    "dev": {
      "passThroughEnv": ["PORT", "TERM_PROGRAM"]
    }
  }
}
